
const routes = {
  register: '/register',
  login: '/login',
  home: '/',
  logout: '/logout',
  following: '/following',
  live: '/live',
  profiles: '/profiles/:id',
  user_id: '/user/:id',
  upload_post: '/upload_post',
};

export default routes;
