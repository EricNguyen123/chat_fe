
const routes = {
  register: '/register',
  login: '/login',
  home: '/',
  logout: '/logout',
  following: '/following',
  live: '/live',
  profiles: '/profiles/:id',
  user_id: '/user/:id',
};

export default routes;
